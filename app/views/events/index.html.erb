<%= render 'layouts/sidebar_menu' %>
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h3 class="page-header">Events</h3>
                </div>
                <!-- /.col-lg-12 -->
            </div>

             <div class="row">
                 <div class="col-lg-12">
              <div class="panel panel-default">
                        <div class="panel-heading">
                            Raw Data:
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive" style="overflow-x: hidden">
                                <table id="events-raw-data" class="cell-border table table-striped table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>Patient Name</th>
                                            <th>Trigger Message</th>
											<th>Created At:</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% @events.each do |dataelements| %>
                                        <tr>
                                          <td><%=User.find(dataelements.patient_id).name %></td>
                                          <td><%=dataelements.message %></td>
										  <td><%=dataelements.created_at %></td>
                                        </tr>
                                        <% end %>
                                    </tbody>
                                </table>
                            </div>
                            <!-- /.table-responsive -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
            </div>
            <!-- /.row -->
        </div>
      </div>
        <!-- /#page-wrapper -->
