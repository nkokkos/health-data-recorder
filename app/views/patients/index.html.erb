<%= render 'layouts/sidebar_menu' %>
       <div id="page-wrapper">
           <div class="row">
               <div class="col-lg-12">
                   <h1 class="page-header">My patients:</h1>
               </div>
               <!-- /.col-lg-12 -->
           </div>
           <!-- /.row -->
           <div class="row">
               <div class="col-lg-12">
                   <div class="panel panel-default">
                       <div class="panel-heading">
                        List shows my patients I "follow"
                       </div>
                       <!-- /.panel-heading -->
                       <div class="panel-body">
                           <div class="table-responsive">
                               <table class="table table-hover">
                                   <thead>
                                       <tr>
                                           <!-- <th>Device Id</th> -->
                                           <th>Patient Name</th>
                                           <th>Action</th>
                                       </tr>
                                   </thead>
                                   <tbody>
                                     <% @patients.all.each do |patient| %>
                                         <tr>
                                           <td><%=find_patient_username(patient.patient_id)%></tb>
                                           <td><%=link_to "View Patient Profile", patient_path(patient.patient_id), { :class=>"btn btn-primary btn-sm" } %></td>
                                          </tr>
                                        <% end %>
                                   </tbody>
                               </table>
                           </div>
                           <!-- /.table-responsive -->
                       </div>
                       <!-- /.panel-body -->
                   </div>
                   <!-- /.panel -->
               </div>
             </div>
           <!-- /.row -->
           <!-- /.row -->

       <!-- /#page-wrapper -->
</div>
