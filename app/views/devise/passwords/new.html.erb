<body>
<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
	 <div class="text-center">
           <img alt="ELTAB", src=<%=image_url("logo.png")%> />
	  </div>
      <div class="login-panel panel panel-default">
        <div class="panel-heading">
          <h1 class="panel-title">Forgot your Password?</h1>
        </div>
        <div class="panel-body">
        <!-- <form role="form"> -->
        <div class="has-error">
        </div>
		  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), :html => { :method => :post} ) do |f| %>
			<fieldset>
				<div class="form-group">
				  <%= f.input :email, required: true, autofocus: true, :input_html => {:class => "form-control"} %>
				 </div>
         <div class="form-actions">
           <%= f.button :submit, "Send me reset password instructions", class: 'button right' %>
         </div>
				<!-- <div class="checkbox">
					<label>
					  <%= f.input :remember_me,label: "Remember Me" , boolean_style: :inline, as: :boolean if devise_mapping.rememberable?
					  %>
					</label>
				</div>
      -->
			  </fieldset>
				<!-- Change this to a button or input when using this as a form -->
        <% end %>
				  <div class="footer">
					  <%= render "devise/shared/links" %>
				 </div>
		<!-- </form> -->
          </div>
            </div>
        </div>
    </div>
</div>
</body>
